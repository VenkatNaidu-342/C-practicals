#include<stdio.h>
int fact(int n);
int main()
{
    int n;
    printf("Enter any number: ");
    scanf("%d",&n);
    printf("Factorial of given number = %d",fact(n));
}
int fact(int n)
{
    if(n==0)
    {
        return 1;
    }
    else
    {
        return n * fact(n - 1);
    }
}
